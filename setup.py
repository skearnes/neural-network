from setuptools import setup, find_packages


def main():
    setup(
        name='neural-network',
        version='0.1',
        license='3-clause BSD',
        url='https://github.com/skearnes/neural-network',
        packages=find_packages(),
    )

if __name__ == '__main__':
    main()
